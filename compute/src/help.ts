
export const help_text = `

Usage:  compute [OPTIONS]

A Cross-platform compute node manager.

Options:
  -s, --setup string       Setup compute node.
  -c, --config string      Location of client config files (default
                           "~/.dmb/config" or "/etc/dmb/config")
  -v, --version            Print version information and quit
  -h, --help               Help document

      --verbose string     Show all logs.

`;
